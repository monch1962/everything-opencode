#!/usr/bin/env node
/**
 * Project Generator Module for TemplateUtils
 *
 * Project file generation methods: generateReadme, generateGitignore
 */

const fs = require('fs');
const path = require('path');
const TemplateCore = require('./template-core');

class ProjectGenerator {
  /**
   * Generate README file
   */
  static generateReadme(projectPath, variables = {}, options = {}) {
    const { overwrite = false, backup = true } = options;

    const readmePath = path.join(projectPath, 'README.md');

    // Default README template
    const readmeTemplate = `# {{name}}

{{description}}

## Getting Started

### Prerequisites
{{#prerequisites}}
- {{.}}
{{/prerequisites}}
{{^prerequisites}}
- Node.js (for JavaScript/TypeScript projects)
- Python 3.x (for Python projects)
- Go 1.16+ (for Go projects)
- Elixir 1.12+ (for Elixir projects)
{{/prerequisites}}

### Installation
\`\`\`bash
{{installation}}
\`\`\`

### Usage
\`\`\`bash
{{usage}}
\`\`\`

## Project Structure

\`\`\`
{{projectStructure}}
\`\`\`

## Features
{{#features}}
- {{.}}
{{/features}}
{{^features}}
- Feature 1
- Feature 2
- Feature 3
{{/features}}

## Configuration
{{#configuration}}
### {{title}}
{{description}}
\`\`\`{{language}}
{{code}}
\`\`\`
{{/configuration}}

## Development

### Running Tests
\`\`\`bash
{{testCommand}}
\`\`\`

### Building
\`\`\`bash
{{buildCommand}}
\`\`\`

## Deployment

{{deploymentInstructions}}

## License

{{license}}

## Contributing

{{contributingGuidelines}}

## Support

{{supportInformation}}
`;

    // Default variables
    const defaultVariables = {
      name: variables.name || 'Project Name',
      description: variables.description || 'A brief description of your project',
      prerequisites: variables.prerequisites || [],
      installation: variables.installation || 'npm install',
      usage: variables.usage || 'npm start',
      projectStructure:
        variables.projectStructure || 'src/\n  main.js\ntests/\npackage.json\nREADME.md',
      features: variables.features || [],
      configuration: variables.configuration || [],
      testCommand: variables.testCommand || 'npm test',
      buildCommand: variables.buildCommand || 'npm run build',
      deploymentInstructions: variables.deploymentInstructions || 'Instructions for deployment',
      license: variables.license || 'MIT',
      contributingGuidelines: variables.contributingGuidelines || 'Contributing guidelines',
      supportInformation: variables.supportInformation || 'Support information',
    };

    try {
      const result = TemplateCore.generateFile(
        null, // No template file, use inline template
        readmePath,
        defaultVariables,
        { overwrite, backup, createDir: true }
      );

      // For inline templates, we need to write the content directly
      const content = TemplateCore.renderTemplate(readmeTemplate, defaultVariables);
      fs.writeFileSync(readmePath, content, 'utf8');

      return {
        success: true,
        path: readmePath,
        size: content.length,
        variables: Object.keys(defaultVariables),
      };
    } catch (error) {
      throw new Error(`Failed to generate README: ${error.message}`);
    }
  }

  /**
   * Generate .gitignore file
   */
  static generateGitignore(projectPath, language, _options = {}) {
    const gitignorePath = path.join(projectPath, '.gitignore');

    // Language-specific .gitignore templates
    const gitignoreTemplates = {
      go: `# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with 'go test -c'
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories
vendor/`,

      python: `# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/`,

      node: `# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# TypeScript v1 declaration files
typings/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
public

# Vuepress build output
.vuepress/dist

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port`,

      elixir: `# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
{{app}}-*.tar`,

      default: `# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
*.swp
*~
*.sublime-*
*.code-workspace

# IDE files
.vscode/
.idea/
*.iml

# Logs
*.log
logs/

# Temporary files
tmp/
temp/

# Build outputs
dist/
build/
out/
*.exe
*.dll
*.so
*.dylib

# Dependencies
node_modules/
vendor/
`,
    };

    // Get template for language or use default
    const template = gitignoreTemplates[language] || gitignoreTemplates.default;

    try {
      // Write .gitignore file
      fs.writeFileSync(gitignorePath, template, 'utf8');

      return {
        success: true,
        path: gitignorePath,
        size: template.length,
        language: language || 'default',
      };
    } catch (error) {
      throw new Error(`Failed to generate .gitignore: ${error.message}`);
    }
  }
}

module.exports = ProjectGenerator;
